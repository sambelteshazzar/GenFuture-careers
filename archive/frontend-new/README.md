# frontend-new (archived)

This folder is an archived version of the previous experimental frontend at `frontend-new`.
It was moved/copied into `archive/frontend-new` during consolidation — the canonical app is now `frontend/`.
